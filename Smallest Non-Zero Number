/////////               Smallest Non-Zero Number             ////////





class Solution {
    public int find(int[] arr) {
        int x = 0;

        for (int i = arr.length - 1; i >= 0; i--) {
            x = (arr[i] + x + 1) / 2;
        }

        return x;
    }
}
